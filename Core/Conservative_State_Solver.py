# Handles the resolution of the conservative flux for each cell

# Requirements : 
# - Should call the flux function, time step function to determine parameters
# - Should check the config files for problems conditions (json files)
# - Should output something usable by the Plot_Handler function 